#include "main.h"

/**
 * _puts - function to print a string
 *
 * @str: string to be printed
 *
 * Return: Always 0
 */

void _puts(char *str)
{
	for (; *str != '\0'; s++)
	{
		_putchar(*str);
	}
	_putchar('\n');
}
